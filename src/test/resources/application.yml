default-bank:
  bank:
    name: "LLC DefaultBank"
    partner:
      prefix-url: "http://localhost:8080"
server:
  port: 8081

spring:
  main:
    allow-bean-definition-overriding: true

  liquibase:
    enabled: false
    change-log: classpath:/db/changelog/db.changelog-master.yaml

  jpa:
    hibernate:
      show-sql: true
      ddl-auto: create-drop
    defer-datasource-initialization: false

  datasource:
    url: jdbc:h2:mem:online_bank_test
    username: test_name
    password: 1234
    hikari:
      allow-pool-suspension: true

    driverClassName: org.h2.Driver

  mail:
    host: smtp.gmail.com
    port: 587
    username: test@email.com
    password: 1234

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        orm:
          jdbc:
            bind: TRACE

jwt:
  lifetime: 15m
  not-before: 1s
  audience: "online_bank audience"
  issuer: "com.example"
  secret-file-name: test_secret_file
  refresh-token-life-time: 30d

app:
  cors:
    allowed-origins: test-url

resend:
  api-key: resend-key
